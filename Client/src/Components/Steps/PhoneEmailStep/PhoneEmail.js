import React from 'react'

export const PhoneEmail = (props) => {
    return (
        <>
            <div>phone and email component</div>
            <button onClick={props.onClick}>Next</button>
        </>
    )
}
